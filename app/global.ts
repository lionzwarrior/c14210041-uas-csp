export type User = {
    id: string,
    username: string,
    password: string,
    role: string
}

export type Product = {
    id: string,
    nama_produk: string,
    harga_satuan: number,
    quantity: number
}
